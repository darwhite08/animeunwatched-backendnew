-- DM inbox: per-participant pin + archive. ADDITIVE.
ALTER TABLE "Conversation"
  ADD COLUMN IF NOT EXISTS "p1Pinned"   BOOLEAN NOT NULL DEFAULT false,
  ADD COLUMN IF NOT EXISTS "p2Pinned"   BOOLEAN NOT NULL DEFAULT false,
  ADD COLUMN IF NOT EXISTS "p1Archived" BOOLEAN NOT NULL DEFAULT false,
  ADD COLUMN IF NOT EXISTS "p2Archived" BOOLEAN NOT NULL DEFAULT false;
